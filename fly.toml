app = "unit-converter-atlas"
primary_region = "fra"

# Removed [http_service] to disable public internet access.
# The app will now only be accessible via the private network.

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
